import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

const title = 'This is a basic React.JS set up for frontend projects';

ReactDOM.render(<App title={title} />, document.getElementById('app'));
